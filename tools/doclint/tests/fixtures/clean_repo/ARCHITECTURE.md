# Architecture (fixture)

A repo-root doc, structurally outside the `docs/` walk until the root is
widened. Its citations use symbol anchors, so they survive edits above them.

The gate is `runner/src/curie_runner/approval.py::authorize_approval`, built
on `runner/src/curie_runner/approval.py::ApprovalGate`.
