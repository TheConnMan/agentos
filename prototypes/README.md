# Prototypes

Historical prototype receipts from the de-risking phase (PT-1 through PT-4).
These are throwaway spikes that proved the risky infrastructure bets before the
MVP was built; they are not shipped code and are not maintained. The de-risking
review and the individual test plans were removed after the MVP shipped and are
preserved in git history (`git log -- docs/`).
