{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agentos.dev/cli/list-agents/v1.json",
  "title": "agentos list-agents --json",
  "description": "The agent-facing payload of `agentos list-agents`: the local bundles discovered under `agents/`, each with its name, description, and directory.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "agents": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "directory": {
            "type": "string"
          }
        },
        "required": [
          "name",
          "description",
          "directory"
        ]
      }
    }
  },
  "required": [
    "agents"
  ]
}
