{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.agentos.dev/cli/secrets/v1.json",
  "title": "agentos secrets list --json",
  "description": "The agent-facing payload of `agentos secrets list`: the names of the secrets stored for the bundle. Only the names are ever emitted -- never a secret value.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "secrets": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "secrets"
  ]
}
